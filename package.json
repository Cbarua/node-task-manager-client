{
  "name": "task-manager-client",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch-path=./ --env-file=./.env --trace-warnings ./src/server.js"
  },
  "keywords": [
    "task-manager",
    "htmx-client"
  ],
  "author": "Chinmoy Barua",
  "license": "ISC",
  "description": "Client app for node-task-manager-api",
  "dependencies": {
    "axios": "^1.7.2",
    "cookie-parser": "^1.4.6",
    "express": "^4.19.2",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "@prettier/plugin-pug": "^3.0.0"
  }
}
